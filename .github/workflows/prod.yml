name: Update Deploy PROD server

on:
  push:
    branches: production

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Connect SSH & Pulling request
        uses: garygrossgarten/github-action-ssh@release
        with:
          host: 153.92.8.220
          username: u153231468
          port: 65002
          privateKey: ${{ secrets.PRIVATE_KEY_PROD }}
          command: |
            cd domains/osborn.id/public_html/laravel/e-catalog/
            git pull
        env:
          CI: true
